# Pytest CICD
A tool for suggesting missing test cases.

## Installation
To install, run `poetry install`.

## Usage
To generate test suggestions, run `python -m pytest_cicd --suggest`.
To apply test suggestions to a file, run `python -m pytest_cicd --apply output_file.py`.
