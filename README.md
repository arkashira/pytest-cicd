# pytest-cicd AI-Driven Test Case Generation
This project generates test cases for Python functions using AI.

## Usage
1. Install the project using `poetry install`.
2. Run the tests using `pytest`.
